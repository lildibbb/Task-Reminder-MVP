### 📁 File Structure

```js
backend/
├── .turbo/ # TurboRepo cache folder (used for monorepos)
├── dist/ # Compiled output files (generated by NestJS)
├── node_modules/ # Project dependencies
├── src/ # Main source code of the application
│ ├── auth/ # Handles authentication logic (e.g., login, registration)
│ ├── config/ # Application configuration files (e.g., environment settings)
│ ├── constant/ # Stores constant values used throughout the app
│ ├── database/ # Database-related logic (e.g., migrations, connection setup)
│ ├── filters/ # Custom exception filters for error handling
│ ├── helper/ # Utility functions or reusable logic
│ ├── interceptor/ # Custom interceptors for modifying requests/responses
│ ├── projects/ # Logic related to the "projects" feature
│ ├── roles/ # Manages user roles and permissions
│ ├── user-roles/ # Handles relationships between users and roles
│ ├── users/ # Logic related to the "users" feature
│ │ ├── dto/ # Data Transfer Objects (define the shape of incoming/outgoing data)
│ │ ├── entities/ # Database entities (define the structure of database tables)
│ │ ├── enums/ # Enumerations (define fixed sets of constants, e.g., roles)
│ │ ├── users.admin.controller.ts # Admin-specific user routes and logic
│ │ ├── users.controller.spec.ts # Unit tests for the users controller
│ │ ├── users.controller.ts # General user-related routes and logic
│ │ ├── users.module.ts # Module bundling controllers, services, and providers
│ │ ├── users.service.spec.ts # Unit tests for the users service
│ │ ├── users.service.ts # Business logic for the users feature
│ ├── app.controller.spec.ts # Unit tests for the main app controller
│ ├── app.controller.ts # Main controller for general routes
│ ├── app.module.ts # Root module of the application
│ ├── app.service.ts # General business logic for the app
│ ├── main.ts # Entry point of the application
├── test/ # Contains test files for the application
├── .env # Environment variables for the application
├── .env.example # Template for environment variables
├── .prettierrc # Configuration for Prettier code formatting
├── eslint.config.mjs # Configuration for ESLint
├── nest-cli.json # Configuration for the NestJS CLI
├── package.json # Project dependencies and scripts
├── README.md # Documentation for the project
```
